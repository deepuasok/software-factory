node_modules/
.next/
*.tsbuildinfo
.DS_Store

# Scaffolded by another local session, reads this machine's Claude Code
# session registry. Not part of the published factory.
apps/session-desk/

# Local settings and secrets stay on each machine. Ship .env.example instead.
.env
.env.*
!.env.example
